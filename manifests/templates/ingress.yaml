---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jpa                  # Ingress name
  labels:
    app: jpa
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "jpa.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jpa               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
